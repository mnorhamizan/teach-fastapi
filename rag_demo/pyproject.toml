[project]
name = "rag-demo"
version = "0.1.0"
description = "Minimal RAG demo: two pipelines (ingest + retrieve/complete) with sqlite-vec and OpenRouter"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.131.0",
    "uvicorn>=0.41.0",
    "sqlalchemy>=2.0.46",
    "openai>=2.21.0",
    "python-dotenv>=1.2.1",
    "sqlite-vec>=0.1.0",
]
